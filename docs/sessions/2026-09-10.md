# Session 2026-09-10

Phase 0: interrogate and decide. No code written.

## Completed

- Read `REQUIREMENTS.md` and `BUILD_PLAN.md`. Read the research repo's portable core
  (`fsm_counter`, `rule_based`, `kinematics_engine`, `pipeline`), its schema version 4
  report emitter and test, one live recording, the pipeline diagram, the mobile FPS
  report, and the latest research session log.
- Verified platform facts: Supabase free and Pro limits, Edge Function limits, Vercel
  function duration, Cloudflare R2 free tier, pgvector untyped columns, the SMTP
  requirement of self-hosted Supabase, Pyodide size.
- Ran find-skills. Worth adopting: `supabase/agent-skills@supabase` (265.8K installs),
  `supabase/agent-skills@supabase-postgres-best-practices` (393.9K),
  `microsoft/playwright-cli@playwright-cli` (148.2K),
  `currents-dev/playwright-best-practices-skill@playwright-best-practices` (80.8K).
  Nothing usable for MediaPipe. Nothing installed this session.
- Grilled in three rounds plus follow-ups. The installed `grill-me` skill points at a
  `grilling` skill that is not installed; its text was fetched from `mattpocock/skills`
  and followed, together with the `domain-modeling` formats for `CONTEXT.md` and ADRs.
- Wrote `CONTEXT.md`, `docs/adr/0001` to `0007`, updated `CLAUDE.md` and `README.md`.

## Decisions made

All recorded in `docs/adr/` and in the "Decided in Phase 0" section of `CLAUDE.md`. In
one line each:

- Counter is correct reps; a set ends at the target, the end-set button, or the attempt cap.
- Workouts own sets; setup asks reps, sets, and rest; a repair set is offered once per
  set with any violation and can be declined; feedback after every set.
- Rule engine client-side in TypeScript, ported by Vern after the Python settles, outside
  these phases, behind the research repo's FrameResult and report contract.
- Exercise configuration is data with a check registry; the "fifth exercise" invariant
  was restated because the research repo's rules are code.
- Self-hosted Supabase Docker stack plus Next.js on a team machine with a free tunnel; no
  SMTP; evaluation by direct database and file access.
- Full raw video plus a separate keypoint file per set, skeleton drawn at playback; all
  33 landmarks stored.
- Two JSONB columns per set: merged attempt records and the engine's report verbatim.
- Similarity and feedback behind JSON contracts and stubs, subject to change; knowledge
  base and expert motions in this Postgres; embedding column deferred to integration.
- Save first, then analyse; feedback nullable with a retry.
- Beep per correct rep and spoken warnings; MediaPipe full model; profile required before
  the first workout; consent in three parts; data kept until the project ends.

## Facts worth remembering

- The research repo counts completed reps in `rep_count`; the app's counter is
  `correct_rep_count`.
- The report's analyse payload carries no landmarks; similarity needs the keypoint file.
- The research repo's live recordings hold no per-frame landmarks; the parity harness
  needs that dump added on the Python side.
- The Python core is still changing weekly; production-ready by end of September is the
  stated goal. The port is Vern's, later.
- The research repo's rules are functions with geometry, not data.

## Open items

- Tunnel choice (Phase 1), keypoint file format (Phase 4), the bound on past workouts in
  the feedback input (Phase 4), embedding model (integration), whether the evaluation is
  supervised, guide videos and thumbnails.

## Next session: Phase 1

- Read `CONTEXT.md`, `CLAUDE.md`, and ADR-0001, 0002, 0005, 0007 first.
- Install and read the two Supabase skills before touching the schema.
- Choose the tunnel. Start the Supabase Docker stack. Migration 1 enables pgvector and
  creates the profile and consent tables with row-level security.
- Consent screen and profile gate before Home.
